[project]
name = "tabletop-dashboard"
version = "0.1.0"
description = "Streamlit dashboard for testing the Tabletop API and managing games."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.32.0",
    "requests>=2.31.0",
    "pytest>=8.0.0",
]
